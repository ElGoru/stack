{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test"
  },
  "dependencies": {
    "sst": "3.16.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.147"
  }
}
