{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test"
  }
}
