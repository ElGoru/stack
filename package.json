{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "settings"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test"
  }
}
